<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="http://schema.phpunit.de/4.1/phpunit.xsd"
         colors="true"
         bootstrap="tests/integration/bootstrap.php"
         debug="true"
>
    <!-- Test suites definition -->
    <testsuite name="Integration Tests">
        <directory suffix="Test.php">tests/integration</directory>
    </testsuite>

    <!-- PHP INI settings and constants definition -->
    <php>
        <ini name="date.timezone" value="Europe/Berlin" />
        <ini name="xdebug.max_nesting_level" value="500" />
    </php>

    <!-- Code coverage filters -->
    <filter>
        <whitelist addUncoveredFilesFromWhiteList="true">
            <directory suffix=".php">src</directory>
        </whitelist>
    </filter>

    <!-- Logging -->
    <logging>
        <log type="coverage-html" target="build/ci/phpunit/coverage/" charset="UTF-8"
             yui="true" highlight="false" lowUpperBound="35" highLowerBound="70" />

        <!--<log type="coverage-clover" target="build/ci/phpunit-integration/clover.xml" />-->

        <!--<log type="junit" target="build/ci/phpunit-integration/phpunit.xml" logIncompleteSkipped="false" />-->
    </logging>
</phpunit>
